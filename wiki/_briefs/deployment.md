## Brief: deployment.md

Focus: How to build, deploy, operate — prerequisites, steps with expected outputs, rollback.

Files to read first:
- `docker-compose.yml`
- `.github/workflows`

Sections (required): Prerequisites / Steps (numbered with expected outputs) / Troubleshooting / Rollback.

Do NOT cover:
- Local dev setup (belongs in `overview.md`)
- CI-only tasks (linking is enough)

Target: 6-10 KB.
